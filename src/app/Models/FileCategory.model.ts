export class FileCategory{

  constructor(Id:number,name:string,count:number, size:number,icon:string,color:string){
       this.Id=Id;
       this.name=name;
       this.count=count;
       this.size=size;
       this.icon=icon;
       this.color=color;
  }
  Id:number;
  name:string;
  count:number;
  size:number;
  icon:string;
  color:string;

}
